[package]
name = "airlock-daemon"
version = "0.4.0"
edition = "2021"

[dependencies]
globset = "0.4.18"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
toml = "1.0"

[lints]
workspace = true

[dev-dependencies]
proptest = "1.10.0"
