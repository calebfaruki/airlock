apiVersion: airlock.dev/v1
kind: AirlockTool
metadata:
  name: git-checkout-branch
spec:
  chamber: workspace-rw
  description: "Create and switch to a new branch"
  image: ghcr.io/calebfaruki/airlock-git:latest
  command: "git checkout -b"
  maxCalls: 1
